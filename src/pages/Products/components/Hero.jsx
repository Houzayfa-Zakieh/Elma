import bg from '../../../assets/images/cover3.jpg';
const Hero = () => {
    return (
        <div className={`w-full min-h-[60vh] bg-no-repeat bg-cover bg-center mt-20`} style={{backgroundImage : `url('${bg}')`}}>

        </div>
    )
}

export default Hero
