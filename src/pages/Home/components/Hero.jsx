import bg from '../../../assets/images/cover.jpg';
const Hero = () => {
    return (
        <div className={`w-full h-[60vh] bg-no-repeat bg-top bg-cover`} style={{backgroundImage : `url('${bg}')`}}>

        </div>
    )
}

export default Hero
